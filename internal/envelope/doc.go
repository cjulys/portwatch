// Package envelope provides a lightweight wrapper that attaches routing
// metadata (priority, tags, free-form key/value pairs) to a set of scanned
// ports before the data is handed off to alerting or reporting pipelines.
package envelope
